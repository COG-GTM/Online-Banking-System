# ===============================
# = TRANSPORT SECURITY
# ===============================

# All traffic must be HTTPS. Set to false only for local development over
# plain HTTP (it also disables the Secure flag on the remember-me cookie).
app.security.require-https = true

# Session cookie hardening
server.servlet.session.cookie.secure = true
server.servlet.session.cookie.http-only = true

# Honour X-Forwarded-* headers so that HTTPS terminated at a reverse proxy is
# detected correctly (otherwise the requiresSecure() redirect loops).
server.use-forward-headers = true

# Enable TLS directly on the embedded container instead of terminating it at a
# proxy by providing a keystore:
# server.ssl.enabled = true
# server.ssl.key-store = classpath:keystore.p12
# server.ssl.key-store-type = PKCS12
# server.ssl.key-store-password = ${SSL_KEYSTORE_PASSWORD}
# server.ssl.key-alias = onlinebanking

# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

# Connection url for the database "netgloo_blog"
spring.datasource.url = jdbc:mysql://localhost:3306/OnlineBankingSystem

# Username and secret
spring.datasource.username = root
spring.datasource.password = 

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect