# ===============================
# = LOCAL DEVELOPMENT OVERRIDES
# ===============================

# Local databases are disposable: let Hibernate build the schema from the
# entities and echo the generated SQL.
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true
