# ===============================
# = DEVELOPMENT PROFILE
# ===============================

# Local development only: let Hibernate manage the schema and echo SQL.
# Activate with --spring.profiles.active=dev

spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = update
